export type IconProps = {
  size?: number;
  color?: Color;
  className?: string;
};

export type Color = 'primary' | 'success' | 'error';

export type Option = {
  label: string;
  value: string;
};
